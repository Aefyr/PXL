<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="AppTheme.NoStatusBar">
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="ColorPickerSeekBar" parent="Widget.AppCompat.SeekBar">
        <item name="android:thumb">@drawable/colorpicker_seekbar_thumb</item>
        <item name="android:paddingTop">2dp</item>
        <item name="android:paddingBottom">2dp</item>
    </style>

    <style name="PXLImageButton" parent="Widget.AppCompat.ImageButton">
        <item name="android:background">@drawable/sketchbook_style_bg_selector_2</item>
        <item name="android:scaleType">centerCrop</item>
        <item name="android:padding">9dp</item>
        <item name="android:elevation" tools:targetApi="lollipop">@dimen/default_elevation</item>
        <item name="android:layout_margin">8dp</item>
    </style>

    <style name="EditTextResolutionInput" parent="Widget.AppCompat.EditText">
        <item name="android:digits">1234567890</item>
        <item name="android:inputType">numberSigned</item>
    </style>

    <style name="SplashScreen" parent="AppTheme">
        <item name="android:windowBackground">@drawable/splash_screen</item>
        <item name="android:windowFullscreen">true</item>

    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="OverlayActivity" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

</resources>
